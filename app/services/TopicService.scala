class TopicService {
  //map of all the topics
  var topics = Map[Topic]()

  /**
    * add (adding new topic or pushing to the old one)
    * get (check if topic exists, in not return error)
    */

}